# Community provider fixtures

These intentionally unsafe, source-only fixtures exercise the Community
scanner's Convex, Vercel, Firebase, Supabase, and generic Node adapters. They
are test inputs, not deployable application code.
