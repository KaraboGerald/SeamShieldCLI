// Deliberately vulnerable fixture module — the key below is fake.
const anthropicKey = "sk-ant-api03-FAKEFIXTUREKEYFAKEFIXTUREKEY00";

export function getAnthropicKey(): string {
  return anthropicKey;
}
